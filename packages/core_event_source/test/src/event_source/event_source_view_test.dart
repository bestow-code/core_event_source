import 'package:test/test.dart';

// import 'main.imports.dart';

main() {
  // late EventSourceView eventSourceView;
  setUp(() {
    // eventSourceView = EventSourceView();
  });
  group('', () {
    test('', () {});
  });
}
