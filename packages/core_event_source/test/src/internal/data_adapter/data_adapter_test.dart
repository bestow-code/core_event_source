import 'package:test/test.dart';

// import 'main.imports.dart';

main() {
  // late DataAdapter dataAdapter;
  setUp(() {
    // dataAdapter = DataAdapter();
  });
  group('', () {
    test('', () {});
  });
}
