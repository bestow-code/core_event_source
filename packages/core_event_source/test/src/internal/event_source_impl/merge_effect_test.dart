import 'package:test/test.dart';

// import 'main.imports.dart';

main() {
  // late MergeEffect mergeEffect;
  setUp(() {
    // mergeEffect = MergeEffect();
  });
  group('', () {
    test('', () {});
  });
}
