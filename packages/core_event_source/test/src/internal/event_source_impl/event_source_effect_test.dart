import 'package:test/test.dart';

// import 'main.imports.dart';

main() {
  // late EventSourceEffect eventSourceEffect;
  setUp(() {
    // eventSourceEffect = EventSourceEffect();
  });
  group('', () {
    test('', () {});
  });
}
