import 'package:test/test.dart';

// import 'main.imports.dart';

main() {
  // late SourceEffect sourceEffect;
  setUp(() {
    // sourceEffect = SourceEffect();
  });
  group('', () {
    test('', () {});
  });
}
